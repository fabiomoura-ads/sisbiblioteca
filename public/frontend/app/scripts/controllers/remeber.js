'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:RemeberCtrl
 * @description
 * # RemeberCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('RemeberCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
